@model BlogListByWriterViewModel
<div class="tech-btm">
    <h4>From Writer</h4>
    @if (Model.Blogs.Count() != 0)
    {
        foreach (var item in Model.Blogs)
        {
            <div class="blog-grids row mb-3">
                <div class="col-md-5 blog-grid-left">
                    <a href="/Blog/Index">
                        <img src="@item.BlogImage" class="img-fluid" alt="">
                    </a>
                </div>
                <div class="col-md-7 blog-grid-right">

                    <h5>
                        <a href="/Blog/Index">@item.BlogTitle</a>
                    </h5>
                    <div class="sub-meta">
                        <span>
                            <i class="far fa-clock"></i>@(((DateTime)item.BlogCreateDate).ToString("ddd-MMM-yyyy"))
                        </span>
                    </div>
                </div>
            </div>
        }
    }
    else
    {
        <p>This writer have not post</p>
    }
</div>